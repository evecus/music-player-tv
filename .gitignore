*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# APK outputs - but keep keystore
app/build/
*.apk
*.aab

# Keep keystore in repo (self-signed, not secret)
!keystore/tvmusic.jks
